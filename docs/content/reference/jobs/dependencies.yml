name: Job Dependencies

jobs:
  default:
    desc: Run everything
    depends_on: [lint, test]
    steps:
      - run: echo "All checks passed"

  lint:
    desc: Check code style
    steps:
      - run: echo "Linting..."

  test:
    desc: Run tests
    steps:
      - run: echo "Testing..."
