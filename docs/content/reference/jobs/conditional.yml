name: Conditional Jobs

vars:
  deploy: false

jobs:
  build:
    aliases: [default]
    desc: Always runs
    steps:
      - run: echo "Building..."

  deploy:
    desc: Only when deploy is true
    if: deploy == true
    depends_on: [build]
    steps:
      - run: echo "Deploying..."
